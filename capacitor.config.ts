import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karinazvereva.flashcards',
  appName: 'European Portuguese Flashcards',
  webDir: 'dist',
};

export default config;
